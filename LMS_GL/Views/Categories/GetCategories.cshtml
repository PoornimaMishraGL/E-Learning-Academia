@model IEnumerable<LMS_GL.Models.Category>

@{
    ViewData["Title"] = "GetCategories";
}

<h1>GetCategories</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.CategId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategId">Delete</a>
                <a asp-action="AssignCourses" asp-route-id="@item.CategId">List Of Courses</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
