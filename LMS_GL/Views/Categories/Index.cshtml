@model IEnumerable<LMS_GL.Models.Category>

@{
    ViewData["Title"] = "Index";
}
 @*<div>
    <table class="table table-striped border">
        <tr>
            <th> @Html.DisplayNameFor(c=>c.CategoryName)</th>
            <th></th>
        </tr>
        
           @foreach(var item in Model){
                <tr>
                    <td>
                        @item.CategoryName
                    </td>
                    <td>
                        <a asp-route-id="@item.CategId" asp-action="AssignCourses">List All Courses</a>
                    </td>
                    <td style="width:150px">
                        <div class="btn-group">
                            <a asp-action="Edit" class="btn btn-primary"  asp-route-id="@item.CategId"><i class="bi bi-pencil-square"></i></a>
                            <a asp-action="Details" class="btn btn-success" asp-route-id="@item.CategId"><i class="bi bi-ticket-detailed"></i></a>
                            <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.CategId"><i class="bi bi-trash3"></i></a>

                        </div>
                    </td>
                </tr>
            }
    </table>
</div>*@

        <div class="container-xxl py-5">
        
            <div class="text-center wow fadeInUp" data-wow-delay="0.1s">
                <h6 class="section-title bg-white text-center text-primary px-3">Categories</h6>
                <h1 class="mb-5">Popular Categories</h1>
                @*<div class="text-xxl-end">
                <a class="btn btn-danger" asp-action="create"><i class="bi bi-plus-circle"></i>&nbsp; Create New</a>
                </div>*@
            </div>
           
            @foreach(var item in Model){
            <div class="row g-4">
                <div class="col-lg-4 col-md-8 wow fadeInUp" data-wow-delay="0.1s">
                    <div class="course-item bg-light">
                        <div class="position-relative overflow-hidden">
                            <img class="img-fluid" src="~/Images/python.jpg" alt="">
                        </div>
                        <div class="text-center p-4 pb-0">
                            <h3 class="mb-0">@item.CategoryName</h3>
                           
                        </div>
                        <div class="col-log-6">
                         <div class="text-end">  
                            <a class="btn btn-info" asp-route-id="@item.CategId" asp-action="AssignCourses"><i class="bi bi-book-half"></i></a>
                         </div>
                        </div>
                        <h2></h2>
                    </div>
                </div>
            </div>
            }    
    </div>

